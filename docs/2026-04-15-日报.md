# 2026-04-15 日报

## 今日完成
- 完成 Unity 项目 Headless/CLI 第一阶段落地：支持 Editor BatchMode 入口与 Player 运行时入口。
- 完成批处理参数链路：场景、跌倒类型、批次、时长、随机种子、输出目录、相机模式/位置/旋转。
- 修复导出数据问题：`frame_XXXX.json` 中 `keypoints` 空数组问题已通过自动绑定相机与骨骼策略修复。
- 完成可执行产物验证：Windows Player 构建成功，并完成 headless 冒烟运行。
- 新增回放工具：提供 Python 脚本回放关键点帧并导出 GIF/MP4，修复回放方向与骨架可视化表现。
- 完成仓库治理：补齐 `.gitignore` 并清理中间产物追踪，分支 `chore/unity-gitignore-cleanup` 已推送。

## 代码变更清单
- 新增：`Assets/Headless/HeadlessBatchRuntime.cs`
- 新增：`Assets/Editor/HeadlessCliEntry.cs`
- 修改：`Assets/BatchFallGenerator.cs`
- 修改：`Assets/CocoExporter.cs`
- 新增：`docs/headless-cli.md`
- 新增：`tools/replay_frames.py`
- 修改：`.gitignore`
- 变更：`Packages/manifest.json`、`Packages/packages-lock.json`、`ProjectSettings/*`（引擎版本与项目设置同步）

## 验证结果
- 编译检查通过（batchmode 返回码 0）。
- Player 构建成功，产物位于 `Output/Player/FallSim.exe`。
- 修复后样例数据 `Output/artifact-keypoint-fix-player/Run_Fall3_0001/frame_0001.json` 的关键点为非空。
- 回放产物：`Output/replay_Run_Fall3_0001.gif`、`Output/replay_Run_Fall3_0001_fixed.gif`。

## 已识别风险
- CLI 对 `key=value` 风格参数兼容性可继续加固（当前推荐 `key value` 传参）。
- 构建/运行日志中仍有少量噪声告警（missing script/tag 等），不影响主流程但建议后续清理。

## 明日计划
- 增强参数解析兼容性并补充回归冒烟命令。
- 清理日志噪声项并补充最小可复现排查记录。
